import React from 'react';

const Home = () => {
	return (
		<div>
			<div>At the Home Page</div>
			<button onClick={()=>console.log('works')}>Test Button</button>
		</div>
	)
};

export default Home;